# App Bootstrap

Owns application startup ordering only (Pinia/i18n/router installation and public-feature bootstrap sequencing).

It must not become a product service locator or a replacement for feature/runtime ownership.
