# Agent Platform — 软件需求

[← 返回软件需求总览](../README.md)

> 本文件是 Agent 正式业务实现的需求入口。详细架构与施工契约仍以 [`../../architecture/agent/ARCHITECTURE.md`](../../architecture/agent/ARCHITECTURE.md) 和 [`../../architecture/agent/IMPLEMENTATION.md`](../../architecture/agent/IMPLEMENTATION.md) 为长期规范源，当前源码接线和验证状态见 [`../../architecture/agent/CURRENT_AGENT_ARCHITECTURE.md`](../../architecture/agent/CURRENT_AGENT_ARCHITECTURE.md)。P1/P2/P3 仍表示历史范围和实现依赖；状态列现在描述当前实现基线，不能把“源码已存在”解释为“完整 E2E 已验收”。

| 需求编号                                    | 功能                                | 详细需求                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 特殊设计 / 适用工程约束                                                                                                                                                                                                                                                                                                                                | 来源                                                                        | 状态                                                 |
| ------------------------------------------- | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------- | ---------------------------------------------------- |
| <a id="srs-agent-001"></a>**SRS-AGENT-001** | App Host 与作用域隔离               | Nexus 必须提供单管理员 Agent Host；App 是数据、授权与能力隔离边界。内置 `nexus.operations` 由稳定 manifest 注册，App 生命周期、grant、settings 与 AppStorage 必须按服务端推导的 `userId/appId` 作用域持久化，不允许调用方覆盖 owner。                                                                                                                                                                                                                                       | App Host 不能向 App 暴露数据库、容器或可变全局 registry；跨 App 访问必须经显式授权。适用 [EC-ARCH-001](../engineering-constraints.md#ec-arch-001) · [EC-ARCH-009](../engineering-constraints.md#ec-arch-009)。                                                                                                                                         | [**FR-AGENT-001**](../traceability/functional-requirements.md#fr-agent-001) | 实现基线已存在；完整E2E待补                          |
| <a id="srs-agent-002"></a>**SRS-AGENT-002** | Provider 与 Agent Settings          | 用户能够配置 OpenAI-compatible Provider、模型能力与 Agent 宿主设置；凭据加密保存，API 不返回明文。无 Provider 时 Agent Host 仍可启动并明确 degraded；软预算、Hard Limits、网络策略与运行能力必须区分展示。                                                                                                                                                                                                                                                                  | 公网 HTTPS 默认允许；私网仅精确例外；metadata/link-local 等硬拒绝。Hard Limit 提升必须二次确认。                                                                                                                                                                                                                                                       | [**FR-AGENT-002**](../traceability/functional-requirements.md#fr-agent-002) | 实现基线已存在；完整E2E待补                          |
| <a id="srs-agent-003"></a>**SRS-AGENT-003** | Thread、Ledger 与 Run               | App 可以创建 Thread 与 Run；同一 Thread 同时最多一个非终态 Run。用户输入、最终消息和工具结果进入统一持久 Ledger；Run 是预算、计划、取消和结果边界，刷新后必须能够从持久事实恢复。                                                                                                                                                                                                                                                                                           | Run/Thread 查询必须校验 `userId/appId`；Resume 不复活旧执行栈。适用 [EC-RUNTIME-003](../engineering-constraints.md#ec-runtime-003)。                                                                                                                                                                                                                   | [**FR-AGENT-003**](../traceability/functional-requirements.md#fr-agent-003) | 实现基线已存在；完整E2E待补                          |
| <a id="srs-agent-004"></a>**SRS-AGENT-004** | Durable Event、SSE 与恢复           | Run 与 Host 都必须提供可重连的 durable event 序列；snapshot + cursor catch-up 不得漏事实。Transient delta 不获得 durable id，断线后不得伪造成 final。启动、reset 与 shutdown 必须让非终态运行安全收敛。                                                                                                                                                                                                                                                                     | SSE cursor、背压、heartbeat、幂等与一次事务提交遵循 Agent 实施 §4/§7；单 Backend/单 scheduler 是一期正式部署基线。                                                                                                                                                                                                                                     | [**FR-AGENT-004**](../traceability/functional-requirements.md#fr-agent-004) | 实现基线已存在；完整E2E待补                          |
| <a id="srs-agent-005"></a>**SRS-AGENT-005** | Operations 只读机器能力             | Phase 1 的 Operations 只能使用已授权的只读诊断与有界文件读取；默认可选择当前及未来连接，但全局 Agent connection denylist 优先拒绝。任何 Phase 1 mutation 必须返回 capability unavailable，而不是偷偷执行。                                                                                                                                                                                                                                                                  | Agent 复用既有 machine capability port，不复用 Workspace 实时 session/cwd/socket。读取仍经过 scope/grant/denylist 检查。                                                                                                                                                                                                                               | [**FR-AGENT-005**](../traceability/functional-requirements.md#fr-agent-005) | 实现基线已存在；完整E2E待补                          |
| <a id="srs-agent-006"></a>**SRS-AGENT-006** | Artifact 与 Files Library           | 用户上传、Agent 输出与证据必须通过 Artifact 管理；ready 前写入、配额预留、hash、rename 与 metadata 提交按崩溃安全流程执行。Agent Hub 提供用户级 Files / Artifact Library，可分页、预览、下载、retain、删除、清理 preview 和显式引用到当前对话。                                                                                                                                                                                                                             | 文件库扩大用户可见性但不扩大 App capability；跨 App 读取仍需显式 grant/link。大对象使用有界 Range，HTML/SVG 不得在 Nexus origin 执行。                                                                                                                                                                                                                 | [**FR-AGENT-006**](../traceability/functional-requirements.md#fr-agent-006) | 实现基线已存在；完整E2E待补                          |
| <a id="srs-agent-007"></a>**SRS-AGENT-007** | Context、Recall 与内置 Skill        | Native Harness 必须按预算构造上下文，工具 schema、Skill body 和 Recall 注入均有数量/字节上限。Phase 1 Memory 仅只读召回已发布事实；内置 Skill 只读仓库内版本化资源，不执行脚本。                                                                                                                                                                                                                                                                                            | 工具输出先脱敏/摘要，大对象进入 Artifact；未消费用户输入和安全/授权状态不能被摘要丢弃。                                                                                                                                                                                                                                                                | [**FR-AGENT-007**](../traceability/functional-requirements.md#fr-agent-007) | 实现基线已存在；完整E2E待补                          |
| <a id="srs-agent-008"></a>**SRS-AGENT-008** | Launcher、Hub 与 Settings UI        | 认证后的应用提供全局 Agent Surface Host、Launcher/Hub、Operations UI、Files Library 和现有 Settings 中的 Agent tab；关闭/最小化/切 App 不等于取消 Run，重新打开通过 snapshot + catch-up 恢复。总开关关闭后隐藏执行入口并拒绝新执行，但保留设置、历史、审计和安全 cleanup。                                                                                                                                                                                                  | 不新增独立 Agent 页面路由；复用现有 shell/overlay/keyboard 基调，App 私有 UI 通过 contribution 接入。                                                                                                                                                                                                                                                  | [**FR-AGENT-008**](../traceability/functional-requirements.md#fr-agent-008) | 实现基线已存在；完整E2E待补                          |
| <a id="srs-agent-009"></a>**SRS-AGENT-009** | Policy、Approval、Lease 与修改闭环  | Phase 2 所有 mutation 必须经过 `inspect → authorize/policy → approval(需要时) → lease → execute → verify` 的统一 ToolExecutor 链。硬 deny 不可被审批覆盖；远端结果未知时进入 reconciliation/quarantine，禁止自动重复 mutation。                                                                                                                                                                                                                                             | Approval 绑定 canonical operation hash；读共享、写互斥；Workspace typed mutation 与 Agent 共用 lease provider。适用 [EC-RUNTIME-005](../engineering-constraints.md#ec-runtime-005)。                                                                                                                                                                   | [**FR-AGENT-009**](../traceability/functional-requirements.md#fr-agent-009) | 实现基线已存在；完整E2E待补                          |
| <a id="srs-agent-010"></a>**SRS-AGENT-010** | Workspace Dev Environment 与 Runner | Phase 2 增加独立 `nexus-agent-runner`。一个 Workspace 是稳定共享项目/文件边界；Node/Python/Go 等作为同一 Workspace Environment 的 Tool family 共存，不按语言创建独立 Environment。全局 Tool Store 支持 `family/version/digest`（digest 按 arch 解析） 多版本并存，每个 Workspace 独立冻结版本组合；切换版本只重建该 Workspace generation，不修改全局 `/usr/bin`、不复制项目文件、不影响其他 Workspace。Runner、Backend 和工作 sandbox 均不得持 Docker socket、启动 dockerd 或 nested Docker。 | Linux sandbox primitive 不可用时 Core 仍可用，但 Environment availability 必须明确 degraded/unavailable 并 fail closed，不能退化成裸进程执行；host Runner 不通过 `privileged`/unconfined Runner 容器规避 sandbox 边界。generation stop/switch 必须终止旧 process/session；Workspace、Tool Pack、cache、quarantine 与 sandbox overhead 分别统计和清理。 | [**FR-AGENT-010**](../traceability/functional-requirements.md#fr-agent-010) | 重构中；host Runner/Workspace generation 远程E2E待验 |
| <a id="srs-agent-011"></a>**SRS-AGENT-011** | Checkpoint 与恢复                   | Checkpoint 只能在没有正在执行工具的安全边界保存计划、输入 watermark、证据和版本；不得保存 live handle、secret 或可继续使用的 lease。Resume 创建新 Run、新 AgentRuntime 和新执行资源，并重新 inspect。                                                                                                                                                                                                                                                                       | 旧审批失效，旧 mutation 不自动重放；恢复缺失 Artifact/环境依赖时明确失败或降级。                                                                                                                                                                                                                                                                       | [**FR-AGENT-011**](../traceability/functional-requirements.md#fr-agent-011) | 实现基线已存在；完整E2E待补                          |
| <a id="srs-agent-012"></a>**SRS-AGENT-012** | MCP、ACP 与 Browser/CDP             | Phase 3 才启用 MCP/ACP/CDP。外部协议工具不得自行声明安全等级绕过本地 Policy；浏览器网页内容属于不可信输入，下载必须转 Artifact 后再交给其他环境处理。                                                                                                                                                                                                                                                                                                                       | MCP server/工具 schema、超时、输入输出均受固定限制；Browser 使用隔离 recipe/profile 与受控 CDP/Gateway。                                                                                                                                                                                                                                               | [**FR-AGENT-012**](../traceability/functional-requirements.md#fr-agent-012) | 部分实现：MCP已接线；ACP/Browser待接线               |
| <a id="srs-agent-013"></a>**SRS-AGENT-013** | Subagent 与 Memory 审核             | Phase 3 支持受统一 Runtime 并发和父 Run 预算约束的 Subagent，每个 profile 可使用独立模型/软预算；通信通过持久 mailbox/shared facts。AI 只能提交 Memory candidate，用户审查发布后才可被召回。                                                                                                                                                                                                                                                                                | delegation 深度、循环/等待图、消息 TTL/背压必须受控；不设置绕过全局并发的独立 child pool。                                                                                                                                                                                                                                                             | [**FR-AGENT-013**](../traceability/functional-requirements.md#fr-agent-013) | 实现基线已存在；完整E2E待补                          |
| <a id="srs-agent-014"></a>**SRS-AGENT-014** | 安装式 App / Skill                  | Phase 3 安装包必须验证 manifest、文件清单、SHA-256 与 Ed25519 签名并防路径穿越/压缩炸弹。动态 Backend App 不得在 Nexus Backend 内 eval/import 任意上传代码，必须进入可停止、可限权的隔离执行单元；动态 UI 使用独立 origin + sandboxed iframe。                                                                                                                                                                                                                              | 升级使用 stage→validate→quiesce→snapshot→migrate→health→atomic activate；迁移失败恢复旧版本，但不宣称回滚已经发生的远端副作用。                                                                                                                                                                                                                        | [**FR-AGENT-014**](../traceability/functional-requirements.md#fr-agent-014) | 实现基线已存在；完整E2E待补                          |
